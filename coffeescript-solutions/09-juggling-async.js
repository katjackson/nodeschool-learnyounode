// Generated by CoffeeScript 1.10.0
(function() {
  var bl, getIt, http, i, len, result, url, urls;

  http = require('http');

  bl = require('bl');

  urls = process.argv.slice(2);

  result = [];

  getIt = function(url, callback) {
    return http.get(url, function(response) {
      return response.pipe(bl((function(err, data) {
        if (err != null) {
          return callback(err);
        }
        return callback(null, data.toString('utf8'));
      })));
    });
  };

  for (i = 0, len = urls.length; i < len; i++) {
    url = urls[i];
    getIt(url, function(err, data) {
      var j, len1, results, x;
      result.push(data);
      if (result.length === urls.length) {
        results = [];
        for (j = 0, len1 = result.length; j < len1; j++) {
          x = result[j];
          results.push(console.log(x));
        }
        return results;
      }
    });
  }

}).call(this);
